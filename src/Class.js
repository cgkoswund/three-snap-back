export default class Class {
  constructor() {
    //to do
    // this.area = area;
  }

  static area(side) {
    return side * side;
  }
}

// let area = (side) => side * side;

// export { area };
